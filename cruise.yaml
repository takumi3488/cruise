command:
  - claude
  - --model
  - "{model}"
  - -p

model: sonnet
plan_model: opus

steps:
  write-tests:
    prompt: |
      Based on the {plan} implementation schedule, please first create the test code, then update the {plan} if necessary.

  implement:
    prompt: |
      Tests have been created according to {plan}. Please implement them to pass. If necessary, update {plan}.

  test:
    command:
      - cargo fmt --all
      - cargo clippy --fix --allow-dirty --all-targets --all-features -- -D warnings
      - cargo test --all-features

  fix-test-error:
    skip: prev.success
    prompt: |
      The following error occurred. Please correct it:
      ---
      {prev.stderr}
    next: test

  verify-completion:
    prompt: |
      Review {plan} and verify that ALL phases/sections have been fully implemented.
      Check the actual codebase against each item in the plan.
      If any phase or section remains unimplemented, implement it now and run the tests.

